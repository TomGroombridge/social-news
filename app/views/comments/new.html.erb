<%=form_for [@post, @comment] do |form| %>
	<%= form.label :name %>
	<%= form.text_field :name %>
	<br>
	<%= form.label :content, 'Comment' %>
	<%= form.text_area :content %>
	<br>
	<%= form.submit %>
<% end %>